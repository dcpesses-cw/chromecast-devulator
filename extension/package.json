{
	"name": "chromecast-emulator-extension",
	"version": "0.1.0",
	"description": "An extension to control a Chromecast Receiver from the browser.",
	"main": "index.mjs",
	"scripts": {
		"extension:build": "node ."
	},
	"keywords": [
		"chromecast",
		"emulator",
		"extension"
	],
	"license": "MIT",
	"devDependencies": {
		"esbuild": "^0.25.0",
		"esbuild-plugin-copy": "^2.1.1",
		"typescript": "^5.0.0"
	}
}
